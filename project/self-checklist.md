# Самопроверка проекта (self-checklist)

Отметьте, что реализовано в вашем проекте.  
Этот чеклист помогает и вам, и преподавателю быстро понять, на каком уровне готовности находится проект.

В столбце «Да/Нет (студент)» поставьте `✅` или `❌`.  
В столбце «Где смотреть / комментарий» укажите файл(ы), где реализован соответствующий пункт.

> Важно: чеклист – это **самооценка**. Преподаватель может не согласиться с вашими отметками и скорректировать оценку.

---

## Таблица самопроверки

| #  | Критерий                                                                 | Да/Нет (студент) | Где смотреть / комментарий                          |
|----|---------------------------------------------------------------------------|------------------|-----------------------------------------------------|
| 1  | Сервис запускается по инструкциям из `project/README.md` и работает      | ✅               | `README.md`, раздел «4. Как запустить проект». Запуск: `python -m src.service.api`, сервис поднимается на `localhost:8000` |
| 2  | Endpoint `/predict` использует **реальную модель**, а не заглушку        | ✅               | `src/service/api.py` — загружает `LandscapePredictor` из `src/inference/predictor.py`, который использует обученную ResNet18. Модель загружается из `artifacts/model_best.pth` при старте |
| 3  | Есть EDA и хотя бы один эксперимент с метриками                          | ✅               | `notebooks/01_eda.ipynb` — распределение классов, примеры изображений, анализ размеров, RGB-распределение. `notebooks/02_experiments.ipynb` — обучение 3 моделей с графиками loss/accuracy |
| 4  | Есть baseline и улучшенная модель, есть **сравнение по метрикам**        | ✅               | `notebooks/02_experiments.ipynb`: Simple CNN (~68%) → ResNet18 Frozen (~89%) → ResNet18 Fine-tuned (~93%). Сравнительные графики и таблица в `report.md`, раздел 5.2 |
| 5  | Код не свален в один ноутбук: есть внятная структура в `src/`            | ✅               | `src/data/dataset.py` — загрузка и аугментация, `src/models/classifier.py` — архитектура ResNet18, `src/training/train.py` — скрипт обучения, `src/inference/predictor.py` — инференс, `src/service/api.py` — FastAPI сервис |
| 6  | Есть Dockerfile **или** понятный сценарий развёртывания без Docker       | ✅               | `Dockerfile` — многоступенчатая сборка на базе `python:3.11-slim`. Также в `README.md` описан запуск без Docker через `python -m src.service.api` |
| 7  | Есть `.env.example` и **нет** в репозитории реальных секретов/паролей    | ✅               | `.env.example` — пример переменных окружения (MODEL_PATH, PORT). Реальных секретов нет, API открытый. Данные и модель в `.gitignore` |
| 8  | Реализованы логи/наблюдаемость (хотя бы консольные логи + `/health`)     | ✅               | `src/service/api.py` — endpoint `/health` возвращает `{"status": "ok", "model_loaded": true/false}`. При старте выводится статус загрузки модели. Ошибки возвращают понятные HTTP-статусы (400, 503) |
| 9  | В `report.md` **обоснован выбор финальной модели** по результатам экспериментов | ✅        | `report.md`, раздел 5.3 — выбор ResNet18 Fine-tuned обоснован: лучшая accuracy (~93%), стабильное обучение, разумный trade-off точность/скорость |
| 10 | `project/README.md` и `report.md` позволяют понять сценарий демонстрации | ✅               | `README.md`, раздел 7 — пошаговый план защиты (EDA → эксперименты → сервис). `report.md`, раздел 9 — сценарий демонстрации с конкретными командами |


